# Copy to selfhost/.env and fill in.
# Cloudflare Zero Trust -> Networks -> Tunnels -> (your tunnel) -> install/run
# -> copy the token from the `--token ...` value.
TUNNEL_TOKEN=paste-your-cloudflare-tunnel-token-here
